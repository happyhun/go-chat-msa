apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo
spec:
  template:
    spec:
      containers:
        - name: mongo
          env:
            - name: GLIBC_TUNABLES
              value: glibc.pthread.rseq=1
