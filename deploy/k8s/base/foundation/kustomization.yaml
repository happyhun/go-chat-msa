apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - local-secret.yaml
  - postgres.yaml
  - mongo.yaml
  - redis.yaml
  - nats.yaml
  - gateway.yaml
  - network-policy.yaml
configMapGenerator:
  - name: nats-config
    files:
      - config/nats.conf
