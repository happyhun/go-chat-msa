apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Render-only inventory. Runtime bootstrap applies phase overlays in order.
resources:
  - namespace.yaml
  - foundation
  - observability
  - migrations
  - apps

